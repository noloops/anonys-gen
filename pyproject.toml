[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "anonys-gen"
version = "0.1.0"
description = "Code generator for the Anonys C++ FSM framework"
requires-python = ">=3.10"

[project.scripts]
anonys-gen = "anonys_gen.cli:main"
